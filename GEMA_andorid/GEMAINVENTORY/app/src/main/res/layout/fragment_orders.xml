<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.orders.OrdersFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Título principal -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/title_orders"
            android:textAppearance="@style/TextAppearance.Material3.HeadlineMedium"
            android:fontFamily="@font/poppins_bold"
            android:layout_marginBottom="16dp" />

        <!-- Lista de pedidos -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/orders_recyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:paddingBottom="120dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_order_card" />

    <LinearLayout
        android:id="@+id/empty_view_orders"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        android:padding="32dp">

        <ImageView
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:src="@drawable/pedi2" 
            android:layout_marginBottom="24dp" 
            android:contentDescription="Empty Orders" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tu lista de pedidos está vacía"
            android:textAlignment="center"
            android:textSize="20sp"
            android:fontFamily="@font/poppins_bold"
            android:textColor="?attr/colorOnSurface" />

        <TextView
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:text="Genera ventas y registra pedidos para gestionarlos desde aquí."
            android:textAlignment="center"
            android:textSize="14sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="32dp"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_empty_create_order"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="32dp"
            android:paddingVertical="12dp"
            android:text="Crear mi primer Pedido"
            app:cornerRadius="24dp"
            app:icon="@drawable/ic_add" />

    </LinearLayout>

    </LinearLayout>

    <!-- Botón flotante -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_order"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="120dp"
        android:src="@drawable/ic_add"
        app:tint="@color/white"
        app:backgroundTint="?attr/colorPrimary"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Full"
        android:contentDescription="Agregar Pedido" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
